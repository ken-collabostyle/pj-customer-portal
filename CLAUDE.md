# AIエージェントの動作方針（ReActパターン）
本プロジェクトでの作業は、推測でいきなり実装・回答をせず、Reasoning → Acting → Observation のサイクルを回して進めてください。

## 1. 推論 (Reasoning)
* 着手前に docs/ 配下の関連仕様（コラボフローAPI、コラボフォームJS API、フォーム構造JSON等）を確認する
* タスクをステップに分解し、着手前に計画をチャットで宣言する
* 複数ステップにまたがる作業計画は docs/plans/ に保存する（テンプレートは docs/plans/README.md を参照）。中断・再開の起点となるため、進捗に応じて随時更新し、docs/plans/manifest.md にも反映する
* **実装作業（コードの追加・変更）に着手する前には、必ずそのステップの詳細計画（変更内容、影響範囲、確認方法）を提示し、ユーザーの承認を得ること。宣言するだけでなく、明示的な承認（「進めてください」等の返答）を得てから着手する。ドキュメント整理や情報収集など、実装を伴わない作業には適用しない**
* 変更が他のフォーム・他モジュールに与える影響を事前に検討する

## 2. 行動 (Acting)
* 段階的に実装し、一度に大きな変更をまとめない。各ステップの着手前には上記の承認プロセスに従う
* 実装後は npm run lint / npm run typecheck / npm test を実行する
* 検証環境での動作確認が必要な場合は、Playwright MCPで実際のコラボフォーム画面を操作し、コンソール・ネットワークを観察する
* コラボフローAPIを利用する場合は docs/collaboflow-api/openapi.yaml を参照し、検証環境に対してリクエストを実行する

## 3. 観察と軌道修正 (Observation & Correction)
* 実行結果（テスト結果、lintエラー、ブラウザコンソール、APIレスポンス）を必ず確認する
* エラー発生時は原因を言語化してから対応する（当てずっぽうの修正をしない）
* 同じ問題で2回以上行き詰まったらアプローチを変えるか、ユーザーに確認する

## 4. 記録 (Recording)
* タスク完了時、docs/agent-records/ に実施記録を残す（テンプレートは docs/agent-records/TEMPLATE.md を参照）
* 記録のフロントマター（type, date, status, tags, related, resolves）を埋め、docs/agent-records/manifest.md に1行追記する
* 過去の未解決課題を解消した場合は、その記録の resolves に対象ファイル名を記載する
* 特に製品改善につながる気づきや課題は必ず記録する
* 対応する docs/plans/ の計画がある場合は、実施記録完了時に計画側の status も completed に更新し、related で相互参照する

## 5. 完了の定義 (Definition of Done)
* 要求された機能が実装されている
* lint/test/typecheckがすべて通っている
* 検証環境での動作確認が完了している（該当する場合）
* 実施記録を保存している

# コミットルール
ユーザーからコミットを指示された場合は、成果物をコミットしてください。コミットの際は以下のルールに従うものとします。
* きりの良い作業単位が完了し、コミットが必要と考えられる場合は、勝手にコミットせず、その旨をユーザーに提案してください。
* コミットメッセージは、変更内容が一目で分かるように具体的に記述してください。
* コミットメッセージは日本語で記述してください。
* コミットは小さな単位で行い、1つのコミットに複数の変更内容を含めないようにしてください。
* コミットメッセージの先頭には、変更内容を表す接頭辞をつけてください。接頭辞は以下の通りです。

## 接頭辞
* feat: 機能追加
* fix: 機能改善・不具合対応
* docs: ドキュメントの変更
* style: インデント整形、空白除去などのコード修正を含まないスタイルの変更
* refactor: リファクタリング（機能追加・不具合対応ではないコードの変更）
* mock: モックアップ関連の追加
* perf: パフォーマンス改善
* test: テストコードの追加・テストコードの改善
* chore: 環境整備（ビルドプロセスやライブラリの追加・更新など）
* delete: 機能削除、ファイル削除