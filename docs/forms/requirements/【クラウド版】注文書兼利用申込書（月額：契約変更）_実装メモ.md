# 実装メモ：【クラウド版】注文書兼利用申込書（月額：契約変更）

[要求事項](【クラウド版】注文書兼利用申込書（月額：契約変更）.md)と[パーツ構造JSON](../json/【クラウド版】注文書兼利用申込書（月額：契約変更）.json)を突合した実装用の対応表・分析メモ。フェーズ1着手前の「推論」フェーズの成果物として作成（[docs/plans/2026-08-24_顧客ポータルカスタマイズ全体計画.md](../../plans/2026-08-24_顧客ポータルカスタマイズ全体計画.md)参照）。

## 重要な発見事項

### 1. 「追加商品名」に商品マスタへのlookupが既に設定済み

`fidAddedProductName`（追加商品名）パーツは`type: lookup`、`master_type: kintone`、`master_name: "【検証用】商品マスタ"`で、以下が自動セットされる設定が**フォーム側に既に存在**する。

| lookup元フィールド（商品マスタ） | セット先パーツ |
|---|---|
| `文字列__1行_`（メーカー型番） | `fidAddedProductCode`（追加商品コード） |
| `標準売価`（定価） | `fidAddedProductUnitPrice`（追加商品単価） |
| `ラジオボタン_0`（単位） | `fidAddedProductUnit`（追加商品単位） |

→ 要求事項「新規追加する際の単価には定価をセットする」は、**このlookupで対応済み**。カスタムJS・プロキシAPIでの実装は不要。ただし「新規追加する際の現契約数に0をデフォルトでセット」（`fidCurrentProductQuantity`）はlookupの対象外のため、JS実装が必要。

### 1.5. 顧客番号はJavaScript側に一切持たせない（2026-08-25 ユーザー確認）

`kintone-contract-db`での検索キーとなる顧客番号は、セキュリティ上の理由（クライアント側で値を書き換えると他社データを不正取得できてしまうため）でJS側に持たせない設計とする。本来はプロキシAPI側でログインユーザーの拡張フィールドから顧客番号を自動差し込む機能を使う想定だが、**この機能はコラボフォームに現時点で未実装**。暫定対応として、プロキシAPIエンドポイントの管理画面側で固定のテスト用顧客番号を設定する（詳細は[docs/kintone/README.md](../../kintone/README.md)参照）。JSからは`collaboform.proxy.call('kintone-contract-db')`を顧客番号なしで呼び出すだけでよい。

### 2. 「インスタンス名」は現状テキスト型 → プルダウン化にはフォーム変更が必要な可能性

`fidContractedInstanceName`（ご契約中のインスタンス名）は`type: text`。要求事項「複数ある場合はプルダウンで選択」を実現するには、JavaScriptでの値操作だけでなく、**コラボフロー側でパーツ種別をリスト等に変更する作業が別途必要になる可能性がある**（コラボフォームJS APIは既存パーツの値・表示状態の操作が中心で、パーツ種別自体の変更はできないため）。

### 3. 「明細」は表示上2つの表だが、データ構造上は1つの`table`にまとまっている（2026-08-25 ユーザー訂正）

コラボフローでは表示上テーブルが2つに分かれていても、パーツ構造JSON上のデータは1つの`table`（`max_row: 10`）にまとまる。実際の表示は以下の2表に分かれる。

- **表1（既存契約変更用）**：`fidProductCancel`（商品解約）, `fidChangedProductName`（変更後商品名）, `fidChangedProductCode`（変更後商品コード）, `fidChangedProductUnitPrice`（変更後商品単価）, `fidCurrentProductQuantity`（現商品契約数）, `fidChangedProductQuantity`（変更後商品契約数）, `fidChangedProductUnit`（変更後商品単位）, `fidChangedProductPrice`（変更後商品価格・自動計算）
- **表2（追加商品用）**：`fidAddedProductName`（追加商品名・lookup）, `fidAddedProductCode`（追加商品コード）, `fidAddedProductUnitPrice`（追加商品単価）, `fidAddedProductQuantity`（追加商品契約数）, `fidAddedProductUnit`（追加商品単位）, `fidAddedProductPrice`（追加商品価格・自動計算）

表1・表2は別々の行を持つのではなく、**同一`table`の1行に表1・表2両方の列がすべて含まれる**（1行＝1レコードに、既存契約変更用フィールドと追加商品用フィールドが同居する）。表示上2つの表に分かれて見えるのはUI側の表示レイアウトであり、JSでのデータ操作時は単一のtable行インデックスとして両方の列を同時に扱う。要求事項の「変更後契約数」は`fidChangedProductQuantity`に対応する。クラウド契約管理DBの`区分`="ベース"の行がベースプランの明細行に相当する。

## パーツ対応表

### 変更適用希望年月

| 要求事項 | パーツID | 現状 | 実装方針 |
|---|---|---|---|
| 当月をデフォルト表示 | `fidChangeApplyYear`, `fidChangeApplyMonth` | `fidChangeApplyMonth`は`list`型、現状デフォルトは固定で「1月」 | `form.show`等で当月の年・月をセット |
| 月末3日前以降は翌月以降のみ選択可（営業日ベース） | `fidChangeApplyMonth` | 全月選択可 | 静的祝日データで営業日を計算し、条件次第で選択肢を制御 |

### お客様情報

| 要求事項 | パーツID | 現状 | 実装方針 |
|---|---|---|---|
| インスタンス名（複数ある場合プルダウン） | `fidContractedInstanceName` | `text`型、必須 | **要検討**：パーツ種別変更の要否をユーザーに確認 |
| 法人／団体名（kintoneから取得） | `fidCorporateName` | `text`型、必須 | kintone連携後にJSでセット |

### ご注文内容（1. 変更後契約数）

| 要求事項 | パーツID | 実装方針 |
|---|---|---|
| 現在ご契約内容（プラン・ユーザー数）表示 | `fidCurrentContractPlan`, `fidCurrentContractUserCount` | kintone-contract-dbから取得してセット |
| 明細に現在契約商品を表示（既存のみ、行追加不可） | `table`の各行（`fidChangedProduct*`, `fidCurrentProductQuantity`） | kintone-contract-dbのレコードをループしてセット（`max_row:10`の制約内） |
| 解約チェックで契約後契約数=0固定 | `fidProductCancel` ⇔ `fidChangedProductQuantity` | チェックON時0固定・入力不可、OFF時元の数量に復元 |

### ご注文内容（2. オプション等新規追加）

| 要求事項 | パーツID | 実装方針 |
|---|---|---|
| 単価に定価をセット | `fidAddedProductName`→`fidAddedProductUnitPrice` | **対応済み**（既存lookup設定） |
| 現契約数に0をデフォルトセット | `fidCurrentProductQuantity`（新規追加行） | **対応不要と判断（2026-09-10）**：`fidCurrentProductQuantity`は①「変更後契約数」セクションの列としてのみ配置されており、②「オプション等新規追加」セクション（`fidAddedProductName`の行）とは視覚的に無関係。同じ行番号を共有する構造だが、②側の新規追加行に対応する①側の`fidCurrentProductQuantity`へ「0」をセットすると、商品名が空欄のまま「現商品契約数＝0」という実体のない行が①セクションに表示されてしまい、かえって紛らわしい。`fidChangedProductPrice`の価格計算式も`fidCurrentProductQuantity`を参照しないため機能的な影響もない。現状（空欄のまま）が正しい挙動と判断し、当初の実装方針を撤回した |

### テスト環境ライセンス（★1）／コラボフォーム（★2・★3）

| 要求事項 | パーツID | 実装方針 |
|---|---|---|
| テスト環境ライセンス新規追加時、★1項目必須化 | `fidTestEnvUrlFirstChoice`等 | 明細行の追加商品が「テスト環境ライセンス」かをJSで判定し`required`を動的制御。**要検討**：商品コード／商品名での判定方法をユーザーに確認 |
| コラボフォーム申込み時、サブドメイン・インスタンス名・規約同意必須化 | `fidCollaboformSubdomain`, `fidContractedInstanceName`, `fidCollaboformTermsOnContract` | 同上の動的判定 |
| コラボフォーム解約時、現サブドメインをkintoneからセット | `fidCollaboformCurrentSubdomain` | kintone-contract-dbから取得 |

### 全般バリデーション

| 要求事項 | 実装方針 |
|---|---|
| オプションのユーザー数がベースライセンスと一致するかチェック | 商品マスタの区分情報等でユーザーライセンス形式か判定し、`form.confirm`等で比較 |
| 同月に別途ユーザー追加があった場合、同月内の減数不可 | **要検討**：「別途ユーザー追加」をどのデータで判定するか（kintone側の履歴か、コラボフロー申請履歴か）が未確定。業務支援チームへの確認が必要（フェーズ2の要検討事項と合わせて確認） |

## 論点の解消状況（2026-08-25 ユーザー確認済み）

1. **インスタンス名のプルダウン化**：フォーム側のパーツ種別は`text`のまま変更せず、JavaScriptで代替UI（`<select>`等をDOM挿入し、選択結果を`parts['fidContractedInstanceName'].value`に反映）を実装する方針。
2. **テスト環境ライセンス／コラボフォームの判別**：現時点では判別ロジックを実装しない（後回し）。フェーズ1では★1・★2・★3の動的必須化は対象外とし、他のステップを先に進める。
3. **「同月別途ユーザー追加」の判定**：データソース未確定。業務支援チームへ確認中のため、[要求事項の要検討事項](【クラウド版】注文書兼利用申込書（月額：契約変更）.md#要検討事項)として保留し、フェーズ2で扱う。フェーズ1のバリデーション実装（ステップe）からは除外する。
