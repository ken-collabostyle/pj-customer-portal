# テスト仕様書：【クラウド版】注文書兼利用申込書（月額：契約変更）フォーム（フェーズ1ステップa）

対象：kintone連携によるインスタンス名UI・現在契約情報の自動表示機能。

- 実装：[src/forms/monthly-contract-change-logic.ts](../../../src/forms/monthly-contract-change-logic.ts)（純粋ロジック）、[src/forms/monthly-contract-change.ts](../../../src/forms/monthly-contract-change.ts)（フォーム専用エントリーポイント、DOM・collaboform API依存）
- 自動テスト：[tests/forms/monthly-contract-change-logic.test.ts](../../../tests/forms/monthly-contract-change-logic.test.ts)（`npm run test`で実行、現行16ケース）
- 関連：[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_インスタンス名UIとkintone連携_実装.md)、[詳細計画](../../plans/2026-08-25_フェーズ1ステップa_インスタンス名UIとkintone連携.md)、[クラウド契約管理DB.md](../../kintone/クラウド契約管理DB.md)

本書は、既存の自動テスト（純粋ロジック単体）が何をカバーしているかの一覧（1章）と、自動テストではカバーされず実機（検証環境）での確認が必要な項目（2章）を整理したもの。

**2026-09-16棚卸し**：本書は当初「2章はすべて未実施」としていたが、[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_実機確認と対応.md)等と突合した結果、TC-17〜TC-26は**TC-20を除き2026-09-02に実機確認済み**（一部は不具合対応込み）であることが判明。2章の各行を実際の結果に更新した。**TC-20（DOM構造不一致時のフェイルセーフの実機確認）のみ、[TC-25・TC-26確認記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-25_TC-26確認.md#L27)に「未実施のまま残存する」と明記されており、現時点でも未実施**（詳細は該当行参照）。

## 1. 自動テストケース一覧（`npm run test`で実施済み）

### 1.1 `toContractRecord`（kintone生レコード → 内部ドメインモデル変換）

| ID | 目的 | 入力 | 期待結果 |
|---|---|---|---|
| TC-01 | フィールドコード対応表経由で全項目を変換できる | `CONTRACT_DB_FIELD_CODES`の全キーに値を持つ生レコード | 対応する`ContractRecord`が得られる |
| TC-02 | 存在しないフィールドは空文字にフォールバックする | 空オブジェクト`{}` | 全プロパティが`""`の`ContractRecord` |

### 1.2 `isActiveMonthlyRecord` / `filterActiveMonthlyRecords`（現在契約中の月額レコード判定）

| ID | 目的 | 入力 | 期待結果 |
|---|---|---|---|
| TC-03 | 月額かつ契約中はtrue | `billingCycle: "月額"`, `contractStatus2: "契約中"` | `true` |
| TC-04 | 年額はfalse | `billingCycle: "年額"` | `false` |
| TC-05 | 契約中以外のステータスはfalse | `contractStatus2: "解約"` | `false` |
| TC-06 | 条件を満たさないレコードを除外する | 正常/年額/解約 混在の3件 | 条件を満たす1件のみ残る |

> **注意（2章TC-14参照）**：上記のとおり現行実装は`billingCycle`と`contractStatus2`のみを条件とし、`区分`（category）は判定に含まれない。

### 1.3 `extractUniqueInstances`（インスタンス名のユニーク抽出）

| ID | 目的 | 入力 | 期待結果 |
|---|---|---|---|
| TC-07 | 重複を除いた出現順のインスタンス名一覧を返す | `["instance-b", "instance-a", "instance-b"]`の順で出現 | `["instance-b", "instance-a"]` |
| TC-08 | 空文字のインスタンス名は無視する | `""`を含むレコード | 空文字を除いた一覧 |
| TC-09 | レコード0件なら空配列 | `[]` | `[]` |

### 1.4 `filterRecordsByInstance` / `findBaseRecord`

| ID | 目的 | 入力 | 期待結果 |
|---|---|---|---|
| TC-10 | 指定インスタンスのレコードのみ返す | 2インスタンス混在 | 指定インスタンス分のみ |
| TC-11 | 区分がベースのレコードを返す | ベース/オプション混在 | ベースレコード |
| TC-12 | ベースレコードがなければundefined | オプションのみ | `undefined` |

### 1.5 `buildCurrentContractSummary` / `buildLineItems`

| ID | 目的 | 入力 | 期待結果 |
|---|---|---|---|
| TC-13 | ベースレコードから法人名・プラン・ユーザー数を組み立てる | ベース+オプション混在 | ベース由来のサマリー |
| TC-14 | ベースレコードがない場合はundefined | オプションのみ | `undefined` |
| TC-15 | 各レコードを明細行データへ変換する | 1件のレコード | `productName`/`productCode`/`unitPrice`/`currentQuantity`を持つ配列 |
| TC-16 | レコード0件なら空配列 | `[]` | `[]` |

## 2. 実機確認が必要な項目（自動テスト対象外・未実施）

以下は純粋ロジックの単体テストではカバーできない、または現行実装と承認済み仕様の間に疑義がある項目。検証環境（テスト用顧客番号設定後）での確認が必要。

| ID | 検証内容 | 確認方法 | 期待結果／判断基準 | 優先度 |
|---|---|---|---|---|
| TC-17 | ~~`isActiveMonthlyRecord`が「区分」を判定条件に含まない仕様相違の実害確認~~ → **解決済み（2026-09-16、仕様相違ではなく意図的設計と確認）**：`isActiveMonthlyRecord`（[logic.ts:70-75](../../../src/forms/monthly-contract-change-logic.ts#L70-L75)）は`区分`を条件に含めないことで、ベース・オプション両方の行を`activeMonthlyRecordsCache`に残す設計。`区分`による絞り込みは後段（`findBaseRecord`によるサマリー抽出、`findUserCountMismatches`によるステップeのユーザー数整合性チェック）で個別に行う。ここで`区分`="ベース"を条件に加えるとオプション行自体が除外され、明細テーブルにオプションが表示されなくなる（要求事項に反する）ため、[詳細計画91行目](../../plans/2026-08-25_フェーズ1ステップa_インスタンス名UIとkintone連携.md#L91)の記述はサマリー抽出（ベース行特定）の条件を指したものであり、`isActiveMonthlyRecord`自体の条件とは別物と整理。**対応不要** | （解決済みのため実機確認不要） | 仕様相違ではなく意図的設計と判断。対応不要 | ー |
| TC-18 | ~~`契約ステータス2`の「切替」ステータスのレコードが誤って現在契約中として扱われないか~~ → **解決済み（2026-09-02）**：対象インスタンスの全レコードが「切替」等の`契約ステータス2`だった場合、`activeMonthlyRecordsCache`が0件になり有効インスタンスの選択肢自体がなくなる不具合を確認。`noActiveContractFound`フェイルセーフ（`alert()`＋申請ブロック）を追加し、実機再確認で解消を確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_実機確認と対応.md#L48)） | （解決済み） | 切替中の行が誤って混入することはない（有効な選択肢がなくなり申請ブロックされる） | ー |
| TC-19 | ~~インスタンス選択プルダウンのDOM構築が実フォームのDOM構造と一致するか~~ → **解決済み（2026-09-02）**：実機確認で(1)プルダウンのフォントサイズ崩れ（生成した`<select>`にMantineのCSSクラス未付与が原因）、(2)単一インスタンス時にインスタンス名が編集可能（読み取り専用化が未実装）の2件を検出し、いずれも修正・再確認完了（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-19プルダウン表示崩れ対応.md)） | （解決済み） | プルダウン表示・選択変更時の再セットとも正常動作を確認 | ー |
| TC-20 | ~~DOM構造不一致時のフェイルセーフ（`alert`表示＋`form.confirm`/`form.submit`での申請ブロック）~~ → **実機テスト見送り（2026-09-16ユーザー判断）**：詳細は下記「2章の補足」参照 | （見送りのため実施しない） | ー（実施しないため判断基準なし） | ー |
| TC-21 | ~~明細テーブル（`tbl_1`）の行数とkintone取得レコード数の大小関係~~ → **解決済み（2026-09-02）**：明細行数がテーブル最大行数を超えた場合に`console.warn`のみで申請可能だった不具合を確認。`lineItemsExceedTableCapacity`フェイルセーフ（`alert()`＋申請ブロック）を追加し、実機再確認で解消を確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-21_TC-23対応.md)） | （解決済み） | 超過時はalert表示＋申請ブロック。不足時は余った行が空欄でクリアされる（この挙動は変更なし） | ー |
| TC-22 | ~~「変更後契約数」の初期値が「現在契約ユーザー数」と同じ値で明細行にセットされるか~~ → **確認済み（2026-09-02、対応不要）**：実機で正常動作を確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-21_TC-23対応.md#L19)） | （確認済み） | `fidChangedProductQuantity`が`fidCurrentProductQuantity`と同じ初期値になっていることを確認 | ー |
| TC-23 | ~~`kintone-contract-db`エンドポイント呼び出し失敗時（プロキシ側エラー、通信エラー）の画面挙動~~ → **解決済み（2026-09-02）**：`console.error`のみで申請可能だった不具合を確認。`kintoneCallFailed`フェイルセーフ（`alert()`＋申請ブロック）を追加し、実機再確認で解消を確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-21_TC-23対応.md)） | （解決済み） | プロキシ側エラー・通信エラーいずれもalert表示＋申請ブロック | ー |
| TC-24 | ~~取得0件（該当顧客番号のアクティブ月額レコードなし）時の画面挙動~~ → **解決済み（2026-09-02、TC-18対応で兼用）**：TC-18で追加した`noActiveContractFound`フェイルセーフが取得0件のケースも同じ分岐でカバーするため追加対応不要と判断（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-21_TC-23対応.md#L25)） | （解決済み） | 取得0件時もalert表示＋申請ブロック（TC-18と同じフェイルセーフ） | ー |
| TC-25 | ~~`CONTRACT_DB_FIELD_CODES`が検証環境のkintoneアプリ73の最新`getFormFields`結果と一致しているか~~ → **確認済み（2026-09-02、対応不要）**：検証環境で`getFormFields`を再取得し全フィールドコード一致をユーザーが確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-25_TC-26確認.md)） | （確認済み） | 全フィールドコード一致 | ー |
| TC-26 | ~~明細の「変更後商品単位」（`fidChangedProductUnit`）が未実装のまま空欄表示されることの影響確認~~ → **確認済み（2026-09-02、対応不要）**：空欄表示のまま運用して問題ないことをユーザーが確認（[実施記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-25_TC-26確認.md)）。**さらに2026-09-11、単位関連パーツ（`fidChangedProductUnit`・`fidAddedProductUnit`）自体がフォームから削除されたため、本項目自体が対象外化**（[実施記録](../../agent-records/2026-09-11_単位パーツ削除の反映.md)） | （確認済み・対象外化） | 空欄表示問題なし（パーツ自体が削除され本項目は消滅） | ー |

### 2章の補足

- **命名注意（TC-17、2026-09-16）**：本書のTC-17とは別に、[2026-09-02の実機確認記録](../../agent-records/2026-09-02_フェーズ1ステップa_実機確認と対応.md#L42)にも「TC-17」という同名の項目があるが、そちらは「区分=ベースのレコードが存在しない場合に法人名等が空欄表示される」不具合（`baseRecordMissing`フェイルセーフで対応済み）を指し、本書のTC-17（`isActiveMonthlyRecord`の条件設計の是非）とは別の論点である。ID体系が本書と実施記録側とで独立して採番されていることに起因する偶然の重複のため、混同しないよう注意。
- **TC-20は実機テストを見送り（2026-09-16ユーザー判断、クローズ）**：[TC-25・TC-26確認記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-25_TC-26確認.md#L27)に「フェイルセーフのDOM構造不一致テスト（TC-20相当）は未実施のまま残存することを明記」とあり、[詳細計画](../../plans/2026-08-25_フェーズ1ステップa_インスタンス名UIとkintone連携.md#L96)の「TC-01〜TC-26をすべて完了」という記載とは矛盾していたため、2026-09-16にユーザーへ対応要否を確認。実機での意図的なDOM改変テストは見送る判断となった。詳細・今後同種の対応が必要になった際の参照先は[実施記録](../../agent-records/2026-09-16_ステップaテスト仕様書棚卸しとTC-20見送り判断.md)参照

## 3. 前提条件

- ~~`kintone-contract-db`エンドポイントのquery固定値（テスト用顧客番号）が検証環境に設定済みであること~~ → **設定済み・実機確認済み（2026-09-02）**：TC-17以降の実機確認はこの設定完了後に実施され、いずれも完了している（[TC-25・TC-26確認記録](../../agent-records/2026-09-02_フェーズ1ステップa_TC-25_TC-26確認.md#L26)参照）
- 検証にはベース／オプション／複数インスタンス／複数ステータスなど、条件を作り分けられる複数の顧客番号データが必要（実施済みのTC-17〜TC-26ではユーザーがこれらの条件を作り分けて確認済み。TC-20を再実施する場合も同様のデータが必要）
